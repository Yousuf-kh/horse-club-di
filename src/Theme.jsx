import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    heading: `'Heading Font Name', Philosopher`,
    body: `'Body Font Name', Philosopher`,
  },
});

export default theme;
